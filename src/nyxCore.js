export class NyxCore {
  constructor() {
    this.version = '0.1.0';
  }

  start() {
    console.log(`Nyx Core v${this.version} is running.`);
    console.log('This repository is now buildable and runnable.');
  }
}
